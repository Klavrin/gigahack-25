import Layout from '@/components/Layout'

const Livestock = ({ isOpen, setIsOpen }: any) => {
  return (
    <Layout isOpen={isOpen} setIsOpen={setIsOpen}>
      Livestock
    </Layout>
  )
}

export default Livestock
